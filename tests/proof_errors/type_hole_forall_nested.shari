type const Prop : Type
local type const u
const imp : Prop → Prop → Prop
infixr → : 25 := imp
const forall.{u} : (u → Prop) → Prop
const foo : Prop
axiom foo_proof : foo

lemma type_hole_forall_nested : ∀ x y, foo :=
  assume foo,
    foo_proof
