type const Prop : Type
const imp : Prop → Prop → Prop
infixr → : 25 := imp
const forall.{u} : (u → Prop) → Prop
const foo : Prop

axiom general : ∀ (x : Prop), foo

lemma type_argument_mismatch : foo := @general[λ (x : Prop → Prop), x]
