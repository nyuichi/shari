[package]
name = "shari-kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { version = "1", features = ["backtrace"] }
once_cell = "1"
regex = "1"
thiserror = "1"

[dev-dependencies]
insta = "1"
